namespace x {
  namespace acc { accumulate namespaces00. }
  :untyped foo :- rev.aux [] [] []. % does not exists, since it is inside acc
}

:untyped
main :- x.acc.rev [1,2,3] [3,2,1], x.acc.rev.aux [] [] [], not(x.foo).
