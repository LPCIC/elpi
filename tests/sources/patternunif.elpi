data tm.

pred q -> (tm -> tm -> tm -> (pred)).
symbol f : tm -> prop.
symbol z : tm.

% q Y should yield Y = \\\f 2
q Y :- pi b\ pi c\ (r :- pi a\ s (f a) => s (X b c a)) => r, Y = X.

main :- q Y, Y = x\y\z\f z.
