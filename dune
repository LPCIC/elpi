(executable
  (name elpi_REPL)
  (public_name elpi)
  (libraries elpi)
  (modules elpi_REPL)
)

(executable
  (name toy)
  (preprocess (per_module
    ((pps ppx_deriving.std elpi.trace.ppx -- --cookie "elpi_trace=\"true\"") toy)))
  (libraries unix)
  (modules toy))

(vendored_dirs vendor)
