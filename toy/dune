(executable
  (name toy)
  (preprocess (per_module
    ((pps ppx_deriving.std elpi.trace.ppx -- --cookie "elpi_trace=\"true\"") runtime ast)))
  (libraries unix)
  (modules runtime parser lexer ast trie discrimination_tree toy))

(ocamllex lexer)
(menhir (modules parser))